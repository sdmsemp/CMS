import React from 'react';

const Dashboard = () => (
  <div className="p-8">
    <h1 className="text-3xl font-bold mb-6">Admin Dashboard</h1>
    {/* Add widgets and links to UserList, ComplaintList, DepartmentForm, etc. */}
  </div>
);

export default Dashboard;
